# Secrets — never commit
*.env
*.env.*
!.env.example
# Local credential / secret notes (e.g. saved deploy logins) — never commit
docs/pass.txt
*secret*.txt
*password*.txt
*credentials*.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.coverage
htmlcov/

# Generated media / local artifacts
output/
generated/
*.mp4
*.png
*.jpg
*.jpeg
*.webp
*.mp3
*.wav
manifests/
*.parquet

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Playwright auth/session state — bearer-equivalent cookies, never commit
# (record_b2.mjs defaults to C:\tmp outside the repo, but guard against overrides)
b2_state*.json
*storageState*.json
