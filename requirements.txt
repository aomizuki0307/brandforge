# BrandForge — Backblaze Generative Media Hackathon
# Pinned loosely for the 2-week build; lock before submission.

# Genblaze SDK: core + B2/S3 storage, with GMI Cloud + OpenAI provider extras.
# OpenAI-first for now (GMI credits pending); GMI kept for the video chain later.
genblaze[gmicloud,openai]>=0.4.1

# Generative fallback + captions
openai>=2.0.0
anthropic>=0.40.0

# Web app
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
jinja2>=3.1.0

# Config / models
pydantic>=2.0.0
python-dotenv>=1.0.0

# Asset index (single Parquet catalog in B2 for the gallery/search layer)
pyarrow>=17.0.0

# Dev / test
pytest>=8.0.0
pytest-cov>=5.0.0
